import { HighlightmovieDirective } from './highlightmovie.directive';

describe('HighlightmovieDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightmovieDirective();
    expect(directive).toBeTruthy();
  });
});
